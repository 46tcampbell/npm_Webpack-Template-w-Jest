# npm_Webpack-Template

This template can be used as a starter for projects that will use npm and webpack along with Jest testing.

sum.js and sum.test.js files have been left as examples and can be deleted when starting new files.
